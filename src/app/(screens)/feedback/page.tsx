import { Metadata } from "next"

export const generateMetadata = () => {
  return {
    title: 'Feedback',
  }
}

const Feedback = () => {
  return (
    <div>Feedback</div>
  )
}

export default Feedback