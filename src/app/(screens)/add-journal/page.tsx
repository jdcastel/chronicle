import { Metadata } from "next"

export const generateMetadata = () => {
  return {
    title: 'Add-Journal',
  }
}

const AddJournal = () => {
  return (
    <div>AddJournal</div>
  )
}

export default AddJournal