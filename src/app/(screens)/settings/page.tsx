import { Metadata } from "next"

export const generateMetadata = () => {
  return {
    title: 'Settings',
  }
}

const Settings = () => {
  return (
    <div>Settings</div>
  )
}

export default Settings