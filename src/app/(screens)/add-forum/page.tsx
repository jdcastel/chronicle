import { Metadata } from "next"

export const generateMetadata = () => {
  return {
    title: 'Add-Forum',
  }
}

const AddForum = () => {
  return (
    <div>AddForum</div>
  )
}

export default AddForum