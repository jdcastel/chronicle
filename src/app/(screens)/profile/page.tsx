import { Metadata } from "next"

export const generateMetadata = () => {
  return {
    title: 'Profile',
  }
}

const Profile = () => {
  return (
    <div>Profile</div>
  )
}

export default Profile