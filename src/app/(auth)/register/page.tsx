import { Metadata } from "next"

export const generateMetadata = () => {
  return {
    title: 'Register',
  }
}

const Register = () => {
  return (
    <div>Register</div>
  )
}

export default Register